function enviarDatos (){
    let nombre = document.getElementById("nombre").value; 
    //longitud de caracteres

    //let lon_caracteres = nombre.length;
    //console.log(lon_caracteres)
    //if(lon_caracteres > 0 ){
    //    console.log("ok")
    // }else
    // document.write("No pasaras!")

}

enviarDatos();

if(nombre === ""){
    console.log(lon_caracteres)
    if(lon_caracteres > 0 ){
        console.log("ok")
     }else
     document.write("No pasaras!")
}